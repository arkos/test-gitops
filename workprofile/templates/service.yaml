apiVersion: v1
kind: Service
metadata:
  name: {{ include "workprofile.fullname" . }}
  labels:
    {{- include "workprofile.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workprofile.selectorLabels" . | nindent 4 }}
